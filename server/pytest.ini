[pytest]
asyncio_mode = auto
# test_client.py is a manual integration script (requires a live server on
# :9000), not an automated pytest suite — exclude it from default collection.
testpaths = test_server.py
